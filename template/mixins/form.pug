mixin form-search(keyword)
    form(
        action ="#" 
        class  = "form form__search"
    )
        input(type="text" placeholder="Tìm kiếm ở đây..." class="")
        button(type="submit" class="")
            i(class="fa-solid fa-magnifying-glass")


//- mixin form-create